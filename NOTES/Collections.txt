										COLLECTIONS

-->Collection(from 1.2)  is a Framework which is extended by Iterable interface(from 1.5 present in java.lang.* package).
-->It is a Heterogenous collection of objects in a single entity.
-->It is present in Java util.* package

further it derives 3 more interface

														<<Iterable>>1.5
															 |
														<<Collections>>1.2
			        ______________________________________________|___________________________________________
                                |                                                                   		  |                                                                      			| 1.5
		         <<List>> 1.2                                                        	   <<Set>> 1.2                                                 		              <<Queue>>---Priority Queue 1.5
				!								        	  !									    			|
	---------------------------------------------      		------------------------------------------------------------------			              		<<Dqueue>1.6 
   	!			!			     !			|						!   				!   							|------Array DQueue 1.6
 ArrayList		  Linked List		Vector 	<<Sorted set>>1.2			Hash Set 			LinkedHash Set				        LinkedList 1.2
	1.2			1.2			   1.0			!					     1.2				1.4
							     |	  	   <<Navigable>>1.6
 							  Stack			|
							     1.0		  Tree Set
											1.2

1.List --- (ArrayList , Linked List , Vector  - classes)
2.Set -- (hash set , linkedhash set (classes) sorted interface -->Tree set  class )
3.Queue - <dqueu>-array queue class and priority queue class


												 LIST 

	-> The List Interface in Java extends the Collection Interface and is a part of the java.util package. It is used to store the ordered collections of elements. 
	->we can organize and manage the data sequentially.
~ Key Features:
	Maintained the order of elements in which they are added.
	Allows duplicate elements.
	The implementation classes of the List interface are ArrayList, LinkedList, Stack, and Vector.
	It can add Null values that depend on the implementation.
	Acessing of elements is by using index 
	Includes the listIterator() method -which can travers list in both foward and backword directions.

a} ArrayList :
	->It is part of the java.util package and implements the List interface. introduced in 1.2
	->inherits Abstract AbstractList and implements List interface
	-> implements  RandomAccess, Cloneable, java.io.Serializable interface
	-> It allows duplicate elements , insertion order is maintained
	-> it allows randomly acess the list
	-> it is not synchronized means not thread safety
	->size is not fixed as array , default size is 10 , when it is filled Array is reintialized in such a way that 
		newCapacity = oldCapacity  + (oldCapacity >>1)
		
	old array values is copied to the new array 	 and old array is dumped

~Key Features 
	ArrayList is Underlined data Structure Resizable Array or Growable Array.
	Duplicates Are Allowed.
	Insertion Order is Preserved.
	Heterogeneous objects are allowed.
	Null insertion is possible

ArrayList is slower than arrays for certain operations, such as inserting elements in the middle of the list.
Increased memory usage
Not thread-safe:
Performance degradation: as size increases

ex : ArrayList<Integer> arr = new ArrayList<>();
	arr.add(10);
	arr.remove(index); --> remove the indexth elem
	arr.remove(Integer.valueOf(10)); --> remove element 10
	arr.clear()  -> clear all the elements
	arr.set(index,value); -> to update
	Collections.sort(arr) -> maually sort the elments -> Collections is a class which have this sort method
	arr.contains(value) - >  value is perest or not (true or false )
	arr.trimToSize() - > The trimToSize() method reduces the capacity of a list to fit exactly the number of items that the list contains.
	for traversing -> usimg for loop 
				using enhanced forloop => for(Integer elem : arr){
										syso(elem); }
				using iterator = > Iterator<Integer> itr = arr.itiratir();
				while(itr.hasNext()){
					syso(itr.next);
				}
Time complexity : for adding - O(n)
			    for removin - O(n) 

USE - ArrayList for storing and accessing data
How the ArrayList works
The ArrayList class has a regular array inside it. When an element is added, it is placed into the array. If the array is not big enough, a new, larger array is created to replace the old one and the old one is removed.
	

b} LinkedList
	->The LinkedList class is a collection which can contain many objects of the same type, just like the ArrayList.
	-> intouduced from 1.2 , implements List, Deque, Cloneable, java.io.Serializable
	-> Underlying data structure is Doubly linked list (Node = previous node addrs - data - next node addres)

How the LinkedList works
The LinkedList stores its elements in "containers." The list has a link to the first container and each container has a link to the next container in the list. To add an element to the list, the element is placed into a new container and that container is linked to one of the other containers in the list.
USE -  to manipulate data. like insertion and deletion

It is is a implenentation of Linked list data structure which is a linear data structure where the elements are not stored in contiguous locations, and every element is a separate object with a data part and an address part.
The elements are linked using pointers and addresses, and each element is known as a node.
A node cannot be acessed directly , we have to start from head and follow the link untill its found

methods : addFirst()	->Adds an element to the beginning of the list	
	addLast() ->	Add an element to the end of the list	
	removeFirst()	->Remove an element from the beginning of the list	
	removeLast()	->Remove an element from the end of the list	
	getFirst()	->Get the element at the beginning of the list	
	getLast() ->	Get the element at the end of the list

advantage and disadvantage
->we can increase or decrease it's size whenever we need, so there is no need to set the size of a linkedlist before using it.
->Adding or removing elements in the middle of a linkedlist is a very simple task. We just need to change the links between nodes without moving other element
->we can move in forward and backward direction

->finding an element is time consuming
->each elemet has extra memory so takes more memory


c}Vector
-> this are legacy classes because prsent since java 1.0
->implements growable array object
-> The directly known subclass is Stack.

Key Features of Vector:
-> It expands as elements are added.
->Vector class is synchronized in nature means it is thread-safe by default.
->Like an ArrayList, it maintains insertion order.
->It allows duplicates and nulls.
->It implements List, RandomAccess, Cloneable, and Serializable.
->By default capacity is 10 and every time count of value increases vector size is doubled  i.e.,  initially 10 then 20  

The synchronization in Vector can lead to slower performance compared to ArrayList

c.a}Stack
-> It implements Stack data structure
-> follows basic principle of LIFO (last-in-first-out)
->The Stack class extends Vector and provide additional implementation like pop,push,peek,empty,search

In java Collection is introduced in version 1.2 and iterable  introduced in 1.5 but how come collection extends interable interface??

1.  Backward Compatibility and Re-architecting:
		 When Java 1.5 was released, it introduced Generics and the enhanced `for-each` loop. To make the `for-each` loop work with existing collection types (like `List`, `Set`, etc., which were already part of the `Collection` hierarchy), a new mechanism was needed.

2.  The Role of `Iterable`:The `Iterable` interface was specifically designed to enable the `for-each` loop. Any class that implements `Iterable` (and thus provides an `iterator()` method) can be used with the `for-each` loop.

3.  Retrofitting `Collection`: Instead of forcing all existing `Collection` implementations to be rewritten to directly implement `Iterable`, the Java designers took a more elegant approach: they made the `Collection` interface itself extend `Iterable`.

    Prior to Java 1.5: The `Collection` interface (and its sub-interfaces like `List` and `Set`) already had an `iterator()` method. This method was part of the original design for iterating through collections using an `Iterator` object.
   

											           SET

The Set Interface is present in java.util package and extends the Collection interface. 
	->No Specific Order:  (Exceptions: LinkedHashSet - because of doubly linked data structure or Hash table and Linked list  and TreeSet - becuse of Navigable interface).
	->Allows One Null Element.
	->Implementation Classes: HashSet, LinkedHashSet, and TreeSet.
	->Thread-Safe Alternatives: For thread-safe operations, use ConcurrentSkipListSet or wrap a set using Collections.synchronizedSet().

The set interface allows the users to perform the basic mathematical operation on the set.Possible operatations are intersection,union,difference

1. HashSet - from 1.2 
	->Can store the Null values.
	->doesn't maintain  insertion order
	->Uses HashMap (implementation of hash table data structure) internally.	
	->implements Serializable and Cloneable interfaces.
	->HashSet is not thread-safe. So to make it thread-safe, synchronization is needed externally.
	->initially capacity is 16
	->load factor is 0.75
	->Before storing an Object, HashSet checks whether there is an existing entry using hashCode() and equals() methods. 
storage :
	Actually the value we insert in HashSet acts as a key to the map Object and for its value, java uses a constant variable(present). So in the key-value pair, all the values will be the same. 
	HashSet s = new HashSet() --> creates map = new HashMap(k,v); in  HashSet() class 
	s.add("geeks"); ---> method boolean add("geeks");
							map.put("geeks");  -- > in storage Geeks stored as Key and value will PRESENT 
like this only unique elements is stored.

2.LinkedHashSet 
	-> It  is an ordered version of HashSet  i.e extended from Hashset
	->combines the functionality of a HashSet with a LinkedList to maintain the insertion order of elements.
	->Stores unique elements only.
	->Maintains insertion order.
	->Provides faster iteration compared to HashSet.
	->Allows null elements.

3. TreeSet
	-> It is a implementation of SortedSet interference
	->uses a Tree(red - black tree) for storage. 
	-> The ordering of the elements is maintained by a set using their natural ordering
	->  does not allow duplicate elements. 
	->It doesn't allow null values and throws NullPointerException because while insertion of null, it gets compared to the existing elements, and null cannot be compared to any value.(from JDK 7 before 1 null can be stored if more are stored then it throws  NullPointerException)
	-> TreeSet implements the NavigableSet interface and provides additional methods to navigate the set (e.g., higher(), lower(), ceiling(), and floor()).
	->It is not thread safe.
	->TreeSet serves as an excellent choice for storing large amounts of sorted information 
	->Objects in a TreeSet are stored in a sorted and ascending order.
An object is said to be comparable if and only if the corresponding class implements a Comparable interface.
String, StringBuffer class and all the Wrapper classes already implements Comparable interface Hence, we DO NOT get a ClassCastException. But if we are creating TreeSet of user defined classes or any Java classes which does not implements comparable interface we will get ClassCastException. to solve this problem we can either implement Comparable to our user defined class or we can pass Comparator object in Constructor while creating the set. because By Default Tree set follows Natiral ordering 



												QUEUE - 1.5

->It stores and processes the data in order means elements are inserted at the end and removed from the front.
->implementations, like PriorityQueue,do not allow null elements.
->  The insertion order is not retained in the PriorityQueue.  The elements are stored based on the priority order which is ascending by default. 
Implementation Classes:
	LinkedList
	PriorityQueue
	ArrayDeque
	ConcurrentLinkedQueue (for thread-safe operations)
->Commonly used for task scheduling, message passing, and buffer management in applications.
	->add(element): Adds an element to the rear of the queue. If the queue is full, it throws an exception.
	   offer(element): Adds an element to the rear of the queue. If the queue is full, it returns false

	->remove(): Removes and returns the element at the front of the queue. If the queue is empty, it throws an exception.
	   poll(): Removes and returns the element at the front of the queue. If the queue is empty, it returns null.

	->element(): Returns the element at the front of the queue without removing it. If the queue is empty, it throws an exception.
	   peek(): Returns the element at the front of the queue without removing it. If the queue is empty, it returns null.


	PRIORITY QUEUE (1.5)
->It is part of the java.util package.extracts AbstractQueue and implements serializable
-> It  processes elements based on their priority rather than the FIFO (First-In-First-Out) concept of a Queue.
->Size of the Priority Queue is dynamic
->PriorityQueue does not permit null.
->It  is not thread-safe, Java provides PriorityBlockingQueue class that implements the BlockingQueue interface to use in a Java multithreading environment.
->The queue retrieval operations poll, remove, peek, and element access the element at the head of the queue.
->It provides O(log(n)) time for add and poll methods.
->default initial capacity


Dequeue 
	-> sub Interface of Queuw , from 1.6, present in java.util
	->Deque is related to the double-ended queue that supports adding or removing elements from either end of the data structure. 
	->It can either be used as FIFO or LIFO
	
ArrayDeque - implements Dequeue
	->It generally provides faster operations than LinkedList because it does not have the overhead of node management.
	->ArrayDeque class uses a resizable array to store its elements, which means that it can grow and shrink dynamically 
	->The ArrayDeque class is a lightweight data structure that does not require additional overhead, such as linked list nodes, making it a good choice for scenarios where memory is limited.
	->Not Thread safe

ArrayDequeue (1.6)


	